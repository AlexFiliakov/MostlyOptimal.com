# CloudFlare Pages redirects
# Note: html_handling = "drop-trailing-slash" in wrangler.toml handles trailing slashes automatically
# The OpenNext worker handles all routing, so explicit redirects are not needed
